"use strict" // treat all js code as new version

// alert(3+3) //we use node.js not browser so that's why it shows the error

let name="Jayesh"
let age=21
let isLoggedin= true
let state;

// int => 2 to the power 23
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined
// symbol => it shows the uniqueness
// object
console.log(typeof undefined)
console.log(typeof null)